version: "3.7"

services: 
  app:
    build: .
    ports:
      - 3000:3000
      - 3007:3007
    # environment:
    #     # default localhost
    #     SERVER_WEBSOCKET: "localhost" 
    #     # default 3007 
    #     WEBSOCKET_PORT: 3008